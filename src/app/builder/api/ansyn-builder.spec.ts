import { AnsynBuilder } from '@builder/api/ansyn-builder';

describe('AnsynBuilder factory', () => {
	let instance: any;
	it('should create a viable instance', () => {
		// instance = new AnsynBuilder({
		// 	id: '1',
		// 	config: {},
		// 	options: {},
		// 	callback: () => {}
		// });
		// expect(instance).toBeTruthy();
	})
});
