import { TimeOptionsIntervalPipe } from './time-options-interval.pipe';

describe('TimeOptionsIntervalPipe', () => {
	it('create an instance', () => {
		const pipe = new TimeOptionsIntervalPipe();
		expect(pipe).toBeTruthy();
	});
});
