import { TimeOptionsPipe } from './time-options.pipe';

describe('TimeOptionsPipe', () => {
	it('create an instance', () => {
		const pipe = new TimeOptionsPipe();
		expect(pipe).toBeTruthy();
	});
});
