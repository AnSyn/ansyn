import { CommunicatorLogMessages } from './communicator-log-messages';

describe('CommunicatorLogMessages', () => {
	it('should create an instance', () => {
		expect(new CommunicatorLogMessages()).toBeTruthy();
	});
});
